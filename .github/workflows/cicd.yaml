name: CICD Pipeline for Frontend 
on:
  push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: test-code
        run: echo "KEY=apex" >> $GITHUB_ENV

      - name: show-env
        run: echo "the value is $KEY or can be written as ${{ env.KEY }}"
