name: test-disbatch-default-branch


on:

  workflow_dispatch:

jobs:
  test:
    name: test-dispatch
    runs-on: ubuntu-latest
    steps:
      - name: hello-default
        shell: bash
        run: echo "hello"
